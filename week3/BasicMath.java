import static java.lang.Math.*;
import java.util.*;

public class BasicMath {
	public static void main(String[] args) {
		/*
		 * double a = -191.635; double b = 43.74; int c = 16, d = 45;
		 *
		 * System.out.printf("The absolute value " + "of %.6f is %.6f%n", a,
		 * abs(a));
		 *
		 * System.out.printf("The ceiling of " + "%.2f is %.7f%n", b, ceil(b));
		 *
		 * System.out.printf("The floor of " + "%.2f is %.0f%n", b, floor(b));
		 *
		 * System.out.printf("The rint of %.2f " + "is %.0f%n", b, rint(b));
		 *
		 * System.out.printf("The max of %d and " + "%d is %d%n", c, d, max(c,
		 * d));
		 *
		 * System.out.printf("The min of %d " + "and %d is %d%n", c, d, min(c,
		 * d));
		 *
		 * System.out.println("\n");
		 *
		 * double x = 11.635; double y = 2.76;
		 *
		 * System.out.printf("The value of " + "e is %.0f%n", Math.E);
		 *
		 * if (Math.E > 2.5) { System.out.println("Closer to 5"); } else {
		 * System.out.println("Closer to 0"); } System.out.printf("exp(%.3f) " +
		 * "is %.3f%n", x, exp(x));
		 *
		 * System.out.printf("log(%.3f) is " + "%.3f%n", x, log(x));
		 *
		 * System.out.printf("pow(%.3f, %.3f) " + "is %.3f%n", x, y, pow(x, y));
		 *
		 * System.out.printf("sqrt(%.3f) is " + "%.3f%n", x, sqrt(x));
		 *
		 * System.out.println("\n\n");
		 *
		 * double degrees = 45.0; double radians = Math.toRadians(degrees);
		 *
		 * System.out.format("The value of pi " + "is %.4f%n", Math.PI);
		 *
		 * System.out.format("The sine of %.1f " + "degrees is %.4f%n", degrees,
		 * Math.sin(radians));
		 *
		 * System.out.format("The cosine of %.1f " + "degrees is %.4f%n",
		 * degrees, Math.cos(radians));
		 *
		 * System.out.format("The tangent of %.1f " + "degrees is %.4f%n",
		 * degrees, Math.tan(radians));
		 *
		 * System.out.format("The arcsine of %.4f " + "is %.4f degrees %n",
		 * Math.sin(radians), Math.toDegrees(Math.asin(Math.sin(radians))));
		 *
		 * System.out.format("The arccosine of %.4f " + "is %.4f degrees %n",
		 * Math.cos(radians), Math.toDegrees(Math.acos(Math.cos(radians))));
		 *
		 * System.out.format("The arctangent of %.4f " + "is %.4f degrees %n",
		 * Math.tan(radians), Math.toDegrees(Math.atan(Math.tan(radians))));
		 */

		// create random object
		/*
		 * Random randomno = new Random();
		 *
		 * // get next next boolean value boolean value =
		 * randomno.nextBoolean();
		 *
		 * // check the value System.out.println("Value is: " + value);
		 *
		 * if (value == true){ System.out.println("Eat my ass");
		 */

		/*
		 * String palindrome = "Terrel Blackston"; int len =
		 * palindrome.length(); char[] tempCharArray = new char[len]; char[]
		 * charArray = new char[len];
		 *
		 * //put the orginal steing in an array of chars
		 *
		 * for (int i = 0; i < len; i++ ) { System.out.println(tempCharArray[i]
		 * = palindrome.charAt(i)); }
		 *
		 * //reverse array of chars for (int j = 0; j < len ; j++ ) {
		 * charArray[j] = tempCharArray[len - 1 - j];
		 *
		 * } String newPalindrom = new String (charArray);
		 *
		 * System.out.println(newPalindrom);
		 */

		// this program requires two
		// arguments on the command line
		//if (args.length == 2) {
			// convert strings to numbers
			/*float a = (Float.valueOf(args[0])).floatValue();
			float b = (Float.valueOf(args[1])).floatValue();
      */


      /*float a = Float.parseFloat(args[0]);
      float b = Float.parseFloat(args[1]);

      System.out.println(a);
      System.out.println(b);
			// do some arithmetic
			System.out.println("a + b = " + (a + b));
			System.out.println("a - b = " + (a - b));
			System.out.println("a * b = " + (a * b));
			System.out.println("a / b = " + (a / b));
			System.out.println("a % b = " + (a % b));


    } else {
			System.out.println("This program " + "requires two command-line arguments.");
		}*/

    /*String a = "hello";
    a =  a + "," + "world";
    System.out.println(a);

    a = "He is" + 33 + "Years old";
    System.out.println(a);
*/
